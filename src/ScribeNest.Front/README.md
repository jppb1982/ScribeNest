# ScribeNest Frontend

Angular 16 frontend for ScribeNest. It consumes the ASP.NET Core API exposed by `src/ScribeNest.Web`.

## Run locally

```bash
npm install
npm start
```

The API base URL is configured in `src/environments/environment.ts`.
